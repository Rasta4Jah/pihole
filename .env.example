# Copy this file to `.env` and set your values.
# Defaults are applied if any variable is omitted.

# WARNING: Never use weak passwords in production.
# This default is for demo/testing; change it before exposing the UI.
PIHOLE_API_PASSWORD=Password!
TZ=America/New_York
DNSMASQ_USER=pihole
PIHOLE_UID=1026
PIHOLE_GID=100
